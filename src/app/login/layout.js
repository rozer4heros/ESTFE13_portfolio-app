export const metadata = {
  title: "Login - Minimal portfolio",
  description: "The Most Fantastic and Flawless Portfolio in the World",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
